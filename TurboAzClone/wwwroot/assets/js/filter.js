
makeInput.addEventListener("change", (params) => {
    make = params.target.value
})
modelInput.addEventListener("change", (params) => {
    model = params.target.value
})
banInput.addEventListener("change", (params) => {
    ban = params.target.value
})
cityInput.addEventListener("change", (params) => {
    city = params.target.value
})
priceMinInput.addEventListener("change", (params) => {
    priceMin = params.target.value
})
priceMaxInput.addEventListener("change", (params) => {
    priceMax = params.target.value
})
valuteInput.addEventListener("change", (params) => {
    valute = params.target.value
})
yearInput.addEventListener("change", (params) => {
    year = params.target.value
})
fuelInput.addEventListener("change", (params) => {
    fuel = params.target.value
})
drivetrainInput.addEventListener("change", (params) => {
    drivetrain = params.target.value
})
gearboxInput.addEventListener("change", (params) => {
    gearbox = params.target.value
})
milageMinInput.addEventListener("change", (params) => {
    milageMin = params.target.value
})
milageMaxInput.addEventListener("change", (params) => {
    milageMax = params.target.value
})
ownerInput.addEventListener("change", (params) => {
    owner = params.target.value
})
seatInput.addEventListener("change", (params) => {
    seat = params.target.value
})
countryInput.addEventListener("change", (params) => {
    country = params.target.value
})