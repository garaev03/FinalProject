@model List<YearGetDto>

@{
    ViewData["Title"] = "Nəqliyyat İlləri";
}

<h1>Nəqliyyat İlləri</h1>

<p>
    <a asp-action="Create" class="btn btn-success text-light">Yeni il əlavə et</a>
</p>
<table class="table" style="vertical-align:middle">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th class="text-center">
                İl
            </th>
            <th>
                Tənzimlənmələr
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td width="50">
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.Value)
                </td>
                <td>
                    <div>
                        <a asp-action="update" asp-route-id="@item.Id">
                            <i class="fa-regular fa-pen-to-square btn btn-warning me-3"></i>
                        </a>
                        <a asp-action="delete" asp-route-id="@item.Id">
                            <i class="fa-solid fa-trash-can btn btn-danger text-light"></i>
                        </a>
                    </div>

                </td>
            </tr>
        }
    </tbody>
</table>

